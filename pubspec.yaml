name: caffeine_core
description: Shared API, config, and models for Caffeine and Caffeine TV apps.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.17.6 <4.0.0"

dependencies:
  http: ^1.1.0
